# EXEMPLO — copie este arquivo para o repositório que você quer proteger,
# em .github/workflows/security.yml, ajustando SUA_ORG.
#
# name: security
# on:
#   pull_request:
#   push:
#     branches: [main]
# permissions:
#   contents: read
#   security-events: write
#   actions: read
#   pull-requests: write   # necessário para o PR decoration (comentário do SecPipe)
# jobs:
#   scan:
#     uses: SUA_ORG/ci_cd/.github/workflows/security-scan.yml@main
#     with:
#       fail_on: critical            # critical | high | medium | low
#       # dast_url: https://app.exemplo.com   # liga o DAST (OWASP ZAP) contra a app rodando
name: example-caller (desativado)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Veja os comentários deste arquivo para o uso real."
